/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './src/components/App';
import {name as TargetSum} from './app.json';

AppRegistry.registerComponent(TargetSum, () => App);

// import {AppRegistry} from 'react-native';

// import App from './src/components/App';

// AppRegistry.registerComponent('TargetSum', () => App);
